export default interface IBanner {
  number: number;
  poll: string;
  edition: number;
  enabled: boolean;
  lastName: string;
  firstName: string;
  bannerName: string;
  era: string;
  branch: string;
  sponsor: string;
  lat: number;
  long: number;
}